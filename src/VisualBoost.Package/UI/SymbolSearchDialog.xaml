<platform:DialogWindow x:Class="VisualBoost.UI.SymbolSearchDialog"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:ui="clr-namespace:VisualBoost.UI"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:platform="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                       xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                       Title="VisualBoost 심볼 탐색"
                       Width="980"
                       Height="560"
                       MinWidth="720"
                       MinHeight="380"
                       ShowInTaskbar="False"
                       WindowStartupLocation="CenterOwner"
                       Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                       PreviewKeyDown="OnPreviewKeyDown">
  <platform:DialogWindow.Resources>

    <Style TargetType="Button">
      <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="9,3" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Surface" CornerRadius="3" Padding="{TemplateBinding Padding}"
                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                    Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Surface" Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.SearchBoxBackgroundKey}}" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ResultItemStyle" TargetType="ListBoxItem">
      <Setter Property="MinHeight" Value="24" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="ItemBorder"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                    BorderThickness="0,0,0,1">
              <GridViewRowPresenter Content="{TemplateBinding Content}"
                  Columns="{Binding View.Columns, RelativeSource={RelativeSource AncestorType=ListView}}"
                  VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ItemBorder" Property="Background"
                        Value="{DynamicResource {x:Static vsshell:VsBrushes.SearchBoxBackgroundKey}}" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="ItemBorder" Property="Background"
                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
                <Setter Property="Foreground"
                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="GridViewColumnHeader">
      <Setter Property="Height" Value="28" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="ToolTip" Value="열 경계를 끌어 너비 비율 조절" />
      <Setter Property="Padding" Value="7,0" />
      <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.GridHeadingBackgroundKey}}" />
      <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}" />
      <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}" />
    </Style>
  </platform:DialogWindow.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Padding="12"
            Background="{DynamicResource {x:Static vsshell:VsBrushes.CommandBarGradientKey}}">
      <Border BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
              BorderThickness="1" CornerRadius="3"
              Background="{DynamicResource {x:Static vsshell:VsBrushes.SearchBoxBackgroundKey}}">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="34" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="34" />
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                     FontSize="15" Text="⌕" />
          <Grid Grid.Column="1">
            <TextBox x:Name="SearchBox" Padding="0,8" FontSize="15" BorderThickness="0"
                     Background="Transparent"
                     Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                     TextChanged="OnSearchTextChanged" />
            <TextBlock x:Name="SearchPlaceholder" VerticalAlignment="Center" Opacity="0.62"
                       Text="심볼명 검색" IsHitTestVisible="False" />
          </Grid>
          <Button x:Name="ClearSearchButton" Grid.Column="2" Margin="4" Padding="0"
                  FontSize="15" Content="×" Visibility="Hidden" ToolTip="검색어 지우기"
                  Click="OnClearSearchClick" />
        </Grid>
      </Border>
    </Border>

    <Grid Grid.Row="1">
      <ui:FittedResultsList x:Name="ResultsList"
               BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
               BorderThickness="1"
               Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
               Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
               ItemContainerStyle="{StaticResource ResultItemStyle}"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               ScrollViewer.VerticalScrollBarVisibility="Visible"
               VirtualizingPanel.IsVirtualizing="True"
               VirtualizingPanel.VirtualizationMode="Recycling"
               MouseDoubleClick="OnResultDoubleClick">
        <ListView.View>
          <GridView AllowsColumnReorder="False">
            <GridViewColumn Header="심볼" Width="460">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Margin="6,0" VerticalAlignment="Center" FontWeight="SemiBold"
                             Text="{Binding Name}" TextTrimming="CharacterEllipsis"
                             ToolTip="{Binding Name}" />
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="파일" Width="290">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Margin="6,0" VerticalAlignment="Center"
                             Text="{Binding FileName}" TextTrimming="CharacterEllipsis"
                             ToolTip="{Binding FullPath}" />
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="라인" Width="60">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Margin="6,0" VerticalAlignment="Center" Text="{Binding Line}" />
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="프로젝트" Width="150">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Margin="6,0" VerticalAlignment="Center" Opacity="0.75"
                             Text="{Binding ProjectName}" TextTrimming="CharacterEllipsis"
                             ToolTip="{Binding ProjectName}" />
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
          </GridView>
        </ListView.View>
      </ui:FittedResultsList>

      <StackPanel x:Name="EmptyStatePanel" 
                  HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
        <TextBlock x:Name="EmptyStateTitle" HorizontalAlignment="Center" FontSize="15"
                   FontWeight="SemiBold" Text="심볼명을 입력하세요." />
        <TextBlock x:Name="EmptyStateDescription" Margin="0,6,0,0" HorizontalAlignment="Center"
                  Opacity="0.68" Text="심볼 이름만 검색합니다." />
      </StackPanel>
    </Grid>

    <Border Grid.Row="2" Padding="8,6"
            Background="{DynamicResource {x:Static vsshell:VsBrushes.CommandBarGradientKey}}"
            BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
            BorderThickness="0,1,0,0">
      <Grid>
        <TextBlock x:Name="StatusText" HorizontalAlignment="Left" Text="0개 결과" />
        <TextBlock HorizontalAlignment="Right" Text="↑↓ 선택   Enter 열기   Esc 닫기" />
      </Grid>
    </Border>
  </Grid>
</platform:DialogWindow>
